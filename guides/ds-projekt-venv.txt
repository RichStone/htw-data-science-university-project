# in terminal
# get current version from git
# go to project source folder
# create environment
conda create --name htw-ds-project --file requirements.txt
# activate virtual environment
source activate htw-ds-project 
# for jupyter integration after activating env:
python -m ipykernel install --user --name htw-ds-project --display-name "Python (htw-ds-project)"

# to exit the environment, just:
source deactivate